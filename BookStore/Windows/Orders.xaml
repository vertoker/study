<Page x:Class="BookStore.Windows.Orders"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BookStore.Windows"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Orders">

    <Grid Background="White">
        <ListView x:Name="OrdersGrid">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <EventSetter Event="PreviewMouseLeftButtonDown" Handler="ListViewItem_PreviewMouseLeftButtonDown"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding id}">Номер заказа</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Address.Name}">Адрес доставки</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Status.Name}">Статус заказа</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding TotalPrice}">Общая цена</GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
